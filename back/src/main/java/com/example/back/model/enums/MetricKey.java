package com.example.back.model.enums;

public enum MetricKey {
    CPU_TOTAL_LOAD,
    CPU_CORE_LOAD,
    MEMORY_PERCENT,
    SWAP_PERCENT,
    DISK_USED_PERCENT,
    GPU_LOAD,
    GPU_MEMORY_USED_PERCENT,
    GPU_TEMPERATURE,
    FAILED_LOGINS
}
